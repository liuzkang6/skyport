# AGENTS.md — 项目开发规范

本文件是 AI 协作开发的行为规范。AI 每次开工前必须通读本文件；本文件条款优先级高于临时指令。
人的职责只有四件：写验收、看报告、手动验证、放行。其余由 AI 和机器执行。

## 0. 使用方式

- AI 开工前先读：AGENTS.md（本文件）→ PRD.md（需求）→ 相关 spec（任务书）→ DESIGN.md（改 UI 时）
- 规矩冲突时：本文件优先；需求冲突时以最新 PRD 为准
- 开工基线：确认 `git status` 干净、`pnpm verify` 绿；发现与任务无关的本地改动，**保留不动**，不自行恢复或清理

## 1. 文档先行（防止文档过期——每个文件都有一条更新规矩）

| 文件 | 什么时候必须先更新它 | 驱动 |
| --- | --- | --- |
| PRD.md | 需求变更时，先增量更新对应章节（只改相关部分），再动 spec 和代码 | §1 |
| spec/<功能>/spec.md | 改行为前，先更新或新建 spec（规则/失败路径/接口/验收）再写码 | §1 |
| DESIGN.md | 改 UI 前，先更新设计规则（新增/变更视觉规则先写进 DESIGN.md）再改组件 | §1 |
| src/errors/errors.ts | 新增错误类型时，先登记错误码再使用 | §3 |
| README.md | 安装方式、命令、架构有变时同步更新 | §1 |

- 文档与代码必须同步提交：一个功能合入时，相关文档一起进同一个 commit
- 禁止"代码改了文档没改"——下次 AI 读旧文档会改回旧行为
- **指令与现状同步**：AGENTS/README/spec 中只保留当前仓库实际存在的功能、命令与文件；删除功能时同步清理全部引用

## 2. 功能开发流程（每次开发走 7 步循环）

- **先诊断后开药**：定位问题时，未明确要求修改代码就先调查原因；结合源码、日志与运行时证据，区分"已确认原因"与"待验证假设"，两者在报告中分开陈述
- **设计缺陷先对齐**：发现设计层缺陷时先与用户对齐方案，不在代码里不断堆兜底分支掩盖

① 写 spec → ② 列失败路径 → ③ 写代码 → ④ 补测试 → ⑤ 跑门禁 → ⑥ 出自测报告 → ⑦ 小步提交

- 第①步 spec 内容 = 产品规则 / 状态所有者 / 接口 / 验收场景 / 失败路径；**涉及状态、时序、异步同步的方案必须附"所有者与事件顺序"图**（文本时序图即可）
- 第②步失败路径至少覆盖：空输入 / 超长 / 权限不足 / 命令不存在 / 超时 / 断网 / 并发
- 第⑥步自测报告用第 9 节模板，如实写，禁止把失败写成通过

## 3. 错误处理

- 错误码集中定义在 src/errors/errors.ts，按域分组；新增错误先登记再使用
- 错误对象字段：type（稳定码）/ message（给人看）/ cause（原始原因）/ context（结构化上下文）/ retryable（是否可重试）
- 错误向上冒泡：底层抛，中间层只在有业务上下文可补充时包装（用 cause 挂住原错误），顶层（入口/中间件）才是唯一 catch 的地方
- 判断错误用错误码，不依赖错误文本（文本会变，码不会）
- 底层业务代码不直接决定退出码、不 process.exit

## 4. 代码规范

- 单文件 ≤ 400 行，超了必须按高内聚低耦合拆分
- 外部 I/O 收敛：所有外部副作用（命令执行/文件/网络/环境变量）必须走统一入口——src/executor/（命令）、src/adapters/（文件/网络等）；业务代码禁止直接调 child_process / fs / fetch / process.env
- **架构红线由机器强制**：`pnpm arch:check`（已并入 verify）检查——services 禁 import fs/child_process/net、禁 process.env/process.exit/fetch；adapters 禁反向依赖 services；process.exit 仅允许出现在 src/cli/index.ts 与 bin/
- **状态纪律**：避免重复状态和多条写入路径；每个状态有唯一所有者与单一写入路径；**禁止用超时掩盖同步问题**（时序错了修时序，不是加 timeout）
- 配置统一前缀 skyport_，优先级：项目配置 > 环境变量 > 默认值；禁止业务代码硬编码
- 常量提取为命名常量，不在业务逻辑散落字面量
- 跨模块交互走公开接口/契约，禁止跨层直接依赖实现
- `pnpm arch:context <模块路径>` 输出模块阅读包（文件/行数/依赖/反向依赖/违规），改不熟悉的模块前先看它

## 5. 测试与门禁

- 行为改动补单元测试；交互改动补 E2E；测试与实现必须一致
- 提交前必须跑：`pnpm typecheck` + `pnpm lint`（完整门禁 `pnpm verify` = typecheck + lint + **arch:check** + test + build），如实报告真实结果（没跑/失败不许写成通过）
- **反假定**：命令与脚本以 package.json 和实际文件为准，不凭记忆假定存在
- 新增外部适配器时，补覆盖失败路径的测试

## 6. 设计规矩（改 UI 前必读 DESIGN.md）

- 遵守 DESIGN.md 的 token 体系：颜色用 --color-*、字号用 text-ui-*，禁止自创颜色/字号/任意像素值
- 组件复用优先：先用 shadcn 基础组件（src/components/ui/），禁止重复造轮子
- 违反 DESIGN.md 视为设计系统缺陷，不是风格偏好；如确需偏离，先在 DESIGN.md 或 spec 记录原因
- 状态收敛：共享状态统一放 store（Zustand），UI 局部状态不得被误当作全局事实

## 7. 日志规范

- 分级语义边界：`debug` 只放高频诊断（协议原始数据、流式 chunk、逐条工具更新），生产不落盘；`info` 放生命周期与一次性事件（进程/会话启停、权限结果、初始化）；`warn` 放可恢复异常；`error` 只放不可恢复错误（崩溃、鉴权丢失、握手失败）
- 日志走 src/logger/（分级 + traceId），不直接 console.log 散落业务代码
- **禁止把凭据、密钥、真实用户数据写入日志、示例或提交**（对齐 webhook 载荷脱敏规矩：宁可少记，不可泄漏）

## 8. 提交规范

- 一个功能一个独立 commit；无关的功能/重构/依赖更新/格式调整不混在一起
- 提交信息前缀：feat: / fix: / refactor: / docs: / test: / chore:
- 提交小到可独立审查、可单独回滚

## 9. 自测报告模板（四段式，每个功能完成必交）

自测报告：<功能名>

- [测了什么] 共 N 条用例，N 条通过（正常 1 条 + 每种失败路径各 1 条）
- [没测什么]（如实写，说明原因）
- [已知风险]
- [手动验证]（你亲手跑的步骤）

报告用中文文字说明，禁止贴大段代码。

## 10. UI 与平台边界（Web UI 落地前即生效，v2 实现时随第一版同步细化）

- Web UI 属 v2 范围；任何 UI 需求动手前，先更新 DESIGN.md 与本节，再写组件
- **分层铁律**：UI（组件/页面）只经 API 层（server 模式提供的 HTTP 接口）访问 services；禁止 UI 直接 import services/adapters/executor，禁止 UI 进程直连 SQLite——数据与执行状态的唯一所有者是 SQLite 与行动状态机，UI 只消费投影
- server 模式只做鉴权、API 编排与会话调度，不保存行动执行状态；将来手机/桌面多端复用同一 server 与会话运行时，不为单端另起执行路径
- 平台差异（桌面/浏览器/移动）经统一平台服务抽象处理（接口注入），不在组件里直接调平台专有 API
- 全局共享状态统一放 store（Zustand）；跨端广播同步的字段（主题、语言等）必须防回环；UI 局部状态不得被误当作服务端事实
- 实时链路与恢复链路语义分开：值守/审批类实时推送与断线重连后的状态恢复是两条路径，改 stream/queue/重连时两种语义都要验证

## 附：文件地图

```text
skyport/
├── AGENTS.md            ← 本文件（开发规范，AI 开工必读）
├── PRD.md               ← 需求活文档（变更先改它）
├── DESIGN.md            ← UI 设计规范（改 UI 前先改它）
├── spec/<功能>/          ← 每个功能的任务书（开发前写）
├── scripts/             ← 架构门禁与模块阅读包（arch:check / arch:context）
├── src/
│   ├── errors/          ← 错误码清单 + 工厂（新增先登记）
│   ├── config/          ← 配置体系（skyport_ 前缀 + 优先级）
│   ├── logger/          ← 日志体系（分级 + traceId）
│   ├── executor/        ← 命令执行层（唯一出口）
│   ├── adapters/        ← 文件/网络等 I/O 适配器
│   ├── services/        ← 业务服务
│   └── cli/             ← CLI 入口（唯一 catch 处）
└── docs/                ← 其他文档
```
