# PRD — skyport（v3：平台全景版）

> 需求活文档：需求变更先增量更新本文件对应章节，再动 spec 与代码（AGENTS.md §1）。
> v3 变更：从 MVP 定义升级为平台全景——业务闭环、七层架构、路线图 v0.3~v0.7、全部内化决策与拍板记录。

## 0. 定位

**skyport 是 AI 运维行动与治理平台：把 AI 与人发起的每一次运维动作，纳入"感知—归并—诊断—决策—执行—验证—学习"的受治理闭环。**

- 产品形态：一体控制面（网关 + serve 后端 + WebUI）+ 可插拔运行时座位（认证制）+ 节点 agent（感官与手臂）
- 部署形态：**私有化部署，覆盖一个公司**（SaaS 多租户后开发，已拍板）
- 核心差异化：治理脊椎（审批门 / 凭证租约 / 审计链 / 风险分级）——调研的七个同类项目均无此脊椎
- 治理哲学：低危零打扰、处置过人、高危逐条过人；风险分级是启发式，身份/上限/留痕/人审是兜底

## 1. 业务闭环（产品的心脏）

### 1.1 主循环：告警 → 恢复

①感知（外部告警 + 节点 agent 自采，双保险）→ ②归并（去重/静默/资产关联/定级，创建事件单）→ ③诊断（AI 载入技能，确定性 analyzer 先跑，只读探查走网关自动执行，输出"已确认/待验证"两栏假说）→ ④决策（提交任务包：方案/风险/时长申报/回滚声明 → 人批准 → 铸时间盒租约）→ ⑤执行（经网关唯一出口：本机/SSH/节点 agent/云 CLI）→ ⑥验证（复检带重试宽限；失败→回滚或升级）→ ⑦升级（AI 卡死/两次失败→交人，附全部上下文）→ ⑧归档（审计链+证据链全可溯）

### 1.2 学习循环：每次事件让下一次更快

事件归档 → 成功路径沉淀为技能修订 / 情景记忆（事件档案：告警指纹→诊断→动作→结果，可查询）→ 高频重复模式进入 L3 候选池。知识资产归平台所有，不绑定运行时。

### 1.3 治理循环：人治理平台本身

审计回看 → 调策略（风险规则/白名单/TTL/档位）→ 管座位（运行时注册/认证/默认/模型配置中心）→ 管权限（三件套/凭证轮换吊销）。

### 1.4 自动化档位（含影子过渡，人踩油门）

| 档位 | 行为 | 门槛 |
| --- | --- | --- |
| L0 人直通 | 人发起，网关留痕 | 默认可用 |
| L1 AI 提议人批 | 全部处置过审批门 | 当前默认 |
| L2 低危自动 | 单段低危只读自动执行 | autoExecLowRisk 策略 |
| L3 已知模式自动处置 | 认证剧本对匹配告警自动跑 | **三相毕业**：训练（只看）→ 影子（记"本应执行"日志，不真执行）→ 检测（真跑）；外加剧本认证+影响面声明+事后审计 |

## 2. 平台架构（七层）

```
⑦ WebUI：Linear 式看板（列=治理状态机）+ Agent 操作台 + 资产/审计/策略页
   + 保险箱界面 + 模型配置中心 + 运行时注册表（参照 Plane）
⑥ 编排层：vendor 的 ZCode workflow 引擎（scheduler/journal/phase 图 1:1 保留，
   node-runner 换受治理版）。单一编排基底 = TS 剧本；节点类型：确定性节点（无 LLM）/
   模型节点/审批门/检查/回滚分支。SKILL.md 仅为知识不承担编排
⑤ 智能层：运行时座位（内置 ZCode fork + 认证制）+ 运维技能库 + 插件体系 + 运行时认证套件
④ 知识层：清单与拓扑（CMDB-lite）+ 常态基线 + 情景记忆 + 态势包（Context Pack）
③ 告警总线：Alerta 模型（event/resource/severity/去重键/open→ack→close）
   + 适配器（Zabbix/Prometheus Alertmanager/通用 webhook）+ ACK SLA 升级链
② 感官与手臂：节点 agent（Teleport 式：仅出站反向隧道/心跳注册/标签/
   能力协商：指标直采+命令执行+文件+日志尾随）+ 云 CLI 通道 + Break-glass 兜底
① 接口层：REST API（唯一权威）+ MCP 适配器（AI 边缘薄翻译）+ WebSocket（数据通道）
〇 躯干：网关（身份/风险/审批/原子状态机/审计链/凭证租约）——已建成并两轮红队验证
```

### 关键机制内嵌

- **凭证三层**（ZCode 工程约束加持）：刷新令牌（skr_，文件 0600，哈希存储）→ 任务租约（审批铸造，时长申报+人批+策略封顶 4h，续期 1 次，闲置 15min 作废）→ 行动审批（租约内逐条风险分级）。轮换双模式（换票即轮换+年龄兜底+复用检测自动吊销+手动命令），单飞锁+代数合并+原子写回，proactive/reactive 续期语义。
- **回滚四类声明**（影响面列表制）：inverse（逆命令）/ snapshot（快照+保质期）/ irreversible（红字+补救说明）/ none（只读）。rollback 生成关联行动走审批，不自动执行。
- **保险箱**：AES-256-GCM 加密存储，主密钥文件可选口令包裹；**注入式用法**（`{{secret:name}}` 引用→executor spawn 时注入 env 即弃，凭证不进命令/日志/审计/通知）。作用域+版本史+轮换提醒+查看高亮审计。
- **Break-glass**：封存应急密钥（口令离线锁物理保险柜，支持 N 取 M 分片），启用即全网最大声报警，默认 4h 自动封存，窗口期操作标记+强制事后报告，**离线可用**（不依赖平台存活）。
- **多人协作**：资产执行互斥（executing 期间新行动串行排队）、事件认领（assignee）、角色四分（viewer/operator/approver/admin，v0.7 随登录落地）、交接班对象（班次+未决快照）。
- **态势包**（平台组装，AI 不从零开始）：资产卡（含拓扑影响面）+ 基线对照 + 近期变更 + 历史档案 + analyzer 结果 + 按需活体探查（输出预算化）。
- **防误判七道闸**：多源交叉（≥2 独立来源）、时效标注（超龄必刷新）、假说/已证实分离、影响面预演、analyzer 前置、dry-run+风险引擎、复检验证。
- **诊断输出纪律**（替代重型预算工程，已砍）：输出截断（已有）+ 结构化摘要先行/按需下钻 + 先过滤再进提示词（grep 摘要不倒原始日志）。
- **诊断双层**（k8sgpt）：确定性 analyzer 注册表先跑（免费可靠），LLM 只解释与深入。
- **告警关联三层**：结构关联（去重键/标签/拓扑/时间窗）→ 模式关联（指纹归并+历史匹配）→ LLM 仅在事件边界（模糊归并/叙事总结/根因假设排序，标注 AI 生成+引用证据链）。逐条实时 LLM 关联不做。
- **提示词注入防御**：观测数据标记为不可信数据永不作为指令（v0.5 spec 必答）；指标签名防投毒。
- **Token 用量治理**：认证运行时强制上报用量（模型/tokens/成本 → usage_events），看板四维出图 + 预算告警进告警总线——AI 的成本也归网关管。
- **插件体系**（ZCode 市场模型改写）：企业技术栈接入 = 插件 = 工具 + 技能包（如 Zabbix 插件、K8s 插件、阿里云插件）；分发渠道分官方市场（内置 + CDN sha256 校验）/个人来源（git/URL/本地目录）；插件启停卸载受治理，安装进审计链。
- **节点 agent 群升级四原则**：版本协商（心跳带版本，控制面支持 N/N-1）→ 灰度批次（1→5%→25%→全量，每批经网关带回滚声明）→ 自动回退看门狗（新版 N 分钟无心跳自动回滚旧版，因坏 agent 收不到远程指令，自救必须无条件）→ 断连缓冲（本地暂存指标，恢复补传）。
- **常驻角色制**（分工即治理单元）：巡查员（定时巡逻/廉价模型/只读）、调查员（态势包消费/诊断/风险上限 low 只能提只读提案——注入防御的权限隔离关键）、处置员（带回滚声明的处置提案/上限 medium+）、审查员（方案合理性复核：通过/有保留/驳回 + 清单打分，**咨询性裁决增强人审不替代**，含与告警指纹相关性核查=注入探测器；与提案者异模型防互捧）。审计员（学习循环档案员：事件归档/情景记忆/治理月报，后批加入）。复杂事件由编排引擎动态会诊（squad）组队。角色 = agent 身份（三件套+模型配置），角色≠运行时。

## 3. 路线图

| 版本 | 主题 | 关键交付 |
| --- | --- | --- |
| **v0.3.x** | 网关完工线 | 凭证三层+轮换双模式 / 回滚声明+快照钩子 / 审计链哈希+事件枚举+单调序 / 高危护栏（强确认/回滚必填/pending 24h 过期/dry-run）/ 僵尸对账 / 执行异步化+结果回推 / 云 CLI 可操作 / CMDB-lite v2（服务+拓扑）/ 保险箱核心 / **REST API v1 + MCP 适配器** / knip+第三方声明+进程树击杀 |
| **v0.4** | 感官与告警 | serve 诞生（守护+自守护+降级策略） / 告警总线（Alerta 模型+三适配器+ACK SLA）/ 节点 agent（Teleport 式全套能力）/ 基线三相训练 / 态势包 v1 / 资产互斥+事件认领 / Break-glass |
| **v0.5** | 智能层 | ZCode fork 内置运行时（认证对接/技能挂载/配置分发三件集成）/ 四角色上线（含审查员）/ 运维技能库首批+诊断双层 analyzer / 多源交叉与置信度规矩 / 注入防御 / 插件体系骨架 / 认证套件与用量上报协议（供第三方座位） |
| **v0.6** | 编排层 | vendor 工作流引擎+受治理 node-runner / L3 剧本（统一引擎 TS 剧本+技能内核）/ 三相毕业落地 / 影子模式日志 |
| **v0.7** | WebUI | 登录+角色四分 / 看板（列=状态机，拖拽=受约束迁移）+ Agent 操作台 / 资产/审计/策略页 / 保险箱界面 / 模型配置中心（统一供应商配置→分发到各运行时，fork 仅作最后手段）/ 运行时注册表 / 交接班 / 治理月报 / Token 用量看板与预算告警 / 插件管理页 / SSE 事件流 |

**验收锚点（不是版本号，是三个第一次）**：第一条外部告警自动进系统并走完闭环；第一个认证运行时自主完成"诊断→提案→被批准→执行"；第一次连续七天无人值守值班。

## 4. 明确不做什么（当前阶段）

- SaaS 多租户与租户隔离（私有化覆盖一个公司；SaaS 后开发）
- 自研时序数据库 / 流式指标存储（节点 agent 直采 + SQLite 起步，量级到再议）
- AI 模型训练（消费模型，不做训练）
- 分布式多控制端（控制面单点 + 自守护 + break-glass 兜底）
- IM 平台特定深度集成（通用 webhook 出站维持）
- 深度 fork 任何运行时（配置→适配器→补丁集 fork 的升级阶梯，fork 是最后手段）
- 节点 agent 之外的 Windows 通道（Windows 支持随后续批次）

## 5. 风险与开放问题（六轴扫描记录）

| 风险 | 对策/状态 |
| --- | --- |
| 提示词注入穿态势包 | v0.5 智能层 spec 必答：内容隔离、不可信数据标记 |
| 指标投毒（agent 被攻陷伪造数据） | 心跳签名+基线训练异常剔除（v0.4 spec 细化） |
| serve 单点 | 自守护+外部心跳告警；**降级策略待拍板**：serve 不可达时 CLI 拒绝服务（安全优先）还是降级直连（可用优先） |
| 数据出境合规 | 模型配置中心支持私有化模型，写明内网部署承诺 |
| 审批不可抵赖 | 多人模式（v0.7）引入强身份；当前 OS 用户名+审计链为记录性方案 |
| 节点 agent 群升级 | 经网关灰度+回滚（吃自己狗粮），v0.4 spec 细化 |
| CLI 层测试盲区（覆盖率 8-12%） | 随各版补 E2E |
| 设计超前于使用（租约语义/影响面列表等期货设计） | spec 内保留，实现时按真实反馈修订，不阻塞主干 |

## 6. 决策记录（累计拍板）

| 决策 | 结论 |
| --- | --- |
| 运行时策略 | 内置自持运行时 = ZCode fork（剥离无关件，自主迭代，上游死活无关）；认证制保留给第三方座位（OpenCode 可后认证，dsh/OpenSRE 观察） |
| 插件体系 | 企业技术栈接入=插件（工具+技能包），ZCode 市场模型改写，启停安装受治理 |
| 智能体组织 | 常驻四角色（巡查/调查/处置/审查）+ 事件驱动会诊；不做万能单脑与微角色化；审查为咨询性，人审是法定门 |
| 接口架构 | REST API 唯一权威，MCP 仅作 AI 边缘薄适配器，内部通信不过 MCP |
| 凭证 | 三层模型+按需时长租约（申报-人批-封顶）+双模式轮换+复用检测 |
| 回滚 | 影响面列表声明制，rollback 走审批不自动执行，通用自动回滚不存在 |
| 保险箱 | 做（v0.3.x 核心），注入式用法，凭证不进命令文本 |
| 兜底 | Break-glass 封存启用制，离线可用，用后必报告 |
| 多人冲突 | 四层解法：原子状态机（已有）/资产互斥/事件认领/角色四分 |
| 多租户 | 私有化部署优先，SaaS 后开发 |
| 编排引擎 | vendor ZCode workflow 引擎改 node-runner，不重写 |
| 自动化配方 | 单一编排基底：vendor 引擎 TS 剧本（Keep YAML 轨撤销，避免三套格式）；保留 Keep 触发器/过滤器概念映射告警订阅 |
| 告警 LLM 用法 | 结构关联先行，LLM 仅事件边界（模糊归并/叙事/根因排序）；逐条实时关联不做 |
| 上下文预算工程 | 砍除，降级为诊断输出纪律三条 |
| Token 用量 | 认证运行时强制上报，看板+预算告警（v0.5 协议 / v0.7 看板） |
| 告警模型 | 直接沿用 Alerta 成熟模型，不自己发明 |
| 模型绑定 | 不 fork 去绑定；模型配置中心统一分发（国产模型均有兼容 API） |
| ZGraph | 撤销（口误），告警适配器打标准栈 |
| 观测深度 | 节点 agent 直采（双保险），不接受仅 TCP 探活的基础面 |

## 7. 参考与内化来源

- ZCode（Apache-2.0）：凭证工程/回滚契约/审计机制/开发规范/设计体系/架构门禁/工作流引擎/技能格式/插件市场——见 `docs/` 与 AGENTS.md/DESIGN.md 内化记录
- Teleport：节点 agent 设计五要素（仅出站/心跳注册/隧道复用/短证书/审计中心化）
- 七项目调研：Keep/Alerta/versus-incident/k8sgpt/HolmesGPT/OpenSRE/Plane——见 `docs/reference-projects.md`
