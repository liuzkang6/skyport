# PRD — skyport（一页纸）

> 需求活文档：需求变更时先增量更新本文件对应章节，再动 spec 与代码（AGENTS.md §1）。

## 1. 解决什么问题

skyport 是类 Linear 的 AI 运维行动与治理平台：把 AI 发起的每一次运维动作统一纳入"登记—审批—执行—审计"闭环，让"AI 做运维"这件事本身可管控、可追溯、可追责。

## 2. MVP 做什么

1. **行动（Action）登记**：把一次 AI 运维动作（命令/脚本）登记为行动单，含意图说明、风险等级与状态流转（待审批 / 执行中 / 成功 / 失败 / 已取消）。
2. **审批门禁**：高危行动须人工批准后才可执行（MVP 以 CLI 交互确认实现）。
3. **受控执行**：行动经统一执行器运行命令，内置超时、退避重试、输出截断与退出码归一化，全过程留痕。
4. **审计与查询**：记录每次执行的操作者、命令、结果、耗时与 traceId，支持按行动单查询历史。

## 3. 明确不做什么（MVP 阶段）

- 不做 Web UI 与多用户权限体系（MVP 为单机 CLI；UI 设计规范先沉淀在 DESIGN.md 备用）
- 不做 AI Agent 本身：skyport 管控 AI 的运维动作，不实现大模型调度与推理
- 不做分布式 / 多节点执行，不做实时监控与告警
- 不对接外部工单 / IM 系统（如 Slack、飞书、Jira）
- 不做密钥管理与凭据保险箱

## 4. 技术选型

TypeScript + pnpm + Node.js（≥ 20）；CLI 形态（commander 解析参数，zod 做运行时校验，vitest 单元测试）；配置统一前缀 skyport_，优先级：项目配置 > 环境变量 > 默认值。
